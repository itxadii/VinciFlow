# AWS SDK (Lambda environment mein hota hai, par local dev ke liye zaroori hai)
boto3>=1.34.0

# API Interactions & X OAuth 2.0 Logic
requests>=2.31.0
requests-oauthlib>=1.3.1

# Gemini AI SDK (Correct package name for your code)
google-generativeai>=0.3.0

# Optional: X API handling made easier (Agar aap raw requests nahi use karna chahte)
tweepy>=4.14.0