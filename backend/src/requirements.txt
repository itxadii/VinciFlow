# AWS SDK (Included in Lambda, but needed for local testing)
boto3==1.34.0

# API Interactions (X/Twitter OAuth 2.0 Flow)
requests==2.31.0

# X (Twitter) Library for Posting & Management
tweepy==4.14.0

# Optional: For advanced OAuth handling if needed
requests-oauthlib==1.3.1